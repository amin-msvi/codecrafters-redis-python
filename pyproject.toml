[project]
name = "codecrafters-redis"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.12.5",
]

[tool.ruff.lint.per-file-ignores]
"app/commands/__init__.py" = ["F401"]

[dependency-groups]
dev = [
    "ty>=0.0.15",
]
